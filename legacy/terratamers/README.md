# Legacy: Terratamers (static game)

This folder preserves the previous contents of the repository (a dependency-free
browser game with a small Node static server). It is not part of the SHIPSCAN
application and is excluded from the Next.js build, lint, and type-check.

Run it standalone with:

```bash
node legacy/terratamers/server.js
```
