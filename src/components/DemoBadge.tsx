/** Intentionally renders nothing: visible demo labels were removed at the owner's request. */
export function DemoBadge(_props: { label?: string; className?: string }) {
  void _props;
  return null;
}
